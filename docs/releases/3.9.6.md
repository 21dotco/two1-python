# `21 version 3.9.6` release notes

*2016-09-28*

`21 version 3.9.6` makes a minor change and fixes a bug from a previous version.

## Changes
- Handle new `flush` error codes from server.

## Bugfixes
- Catch `InsufficientBalanceError` during `buy`.
